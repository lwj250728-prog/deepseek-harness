/**
 * Fiscal-map plugin, node half.
 *
 * Deliberately empty: the fiscal map is a pure browser-surface feature (a
 * Settings section over embedded public dataset). Nothing about it belongs
 * to the host plane — no tools, no model-facing surface, no services.
 */

/** Host plugin body — the web client half composes all UI. */
export function apply(): void {}
