/**
 * Web learning-area plugin, node half.
 *
 * Deliberately empty: the learning area is a pure browser surface over the
 * host's cognition RPC, so this half exists only to complete the dual-face
 * plugin shape the client registry expects.
 */

/** Host plugin body — all behavior lives in the browser half. */
export function apply(): void {}
